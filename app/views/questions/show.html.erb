<div class="page-header">
  <h1>Survey Questions</h1>
</div>

<div class="row">
  <%= form_tag "/user_answers", method: :post do %>
      <div class="col-md-6 col-centered">
        <div class="thumbnail">
          <h3><%= @question.question %></h3>
            <% @question.answers.each do |answer| %>
            <div>
              <button type="submit" class="btn btn-default"><%= hidden_field_tag :answer_id, answer.id %> <%= answer.answer %></button>
            </div>
            <% end %>
            <div>
              <%= hidden_field_tag :question_id, @question.id + 1 %>
            </div> 
            <% if @question.id > 2 %>
              <button class="btn btn-default" type="submit"><%= link_to "Previous", "#{@question.id - 1}" %></button>
            <% end %>
          </div>
        </div>
    <% end %>
  </div>
